name: Validate Renovate Config

on:
  push:
    branches: [main]
    paths: ['*.json', '*.json5']
  pull_request:
    paths: ['*.json', '*.json5']
  workflow_dispatch:

permissions: read-all

jobs:
  validate:
    uses: trowaflo/github-actions/.github/workflows/validate-renovate.yml@0f13bed7ee93c1b8b51ce18fd3a6b9726c22b886 # PR#21 feat/sarif-upload-checkov-trivy
