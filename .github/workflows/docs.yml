name: Auto Generate Documentation

on:
  push:
    branches: [main]
    paths: ['*.json', '*.json5']
  pull_request:
    paths: ['*.json', '*.json5']
  workflow_dispatch:

jobs:
  docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Create Documentation
        run: |
          # Create a comprehensive README
          cat > README.md << 'EOF'
          # 🔧 Renovate Configuration Presets

          This repository contains shared Renovate configuration presets for consistent dependency management across projects.

          ## 📦 Available Presets

          EOF

          # Generate documentation for each preset
          for file in *.json *.json5; do
            if [[ -f "$file" && "$file" != "renovate.json" ]]; then
              name=$(basename "$file" | sed 's/\.[^.]*$//')
              echo "### 🎯 $name" >> README.md
              echo "" >> README.md
              echo "\`\`\`json" >> README.md
              echo "\"extends\": [\"github>trowaflo/renovate-config:$file\"]" >> README.md
              echo "\`\`\`" >> README.md
              echo "" >> README.md

              # Extract description from file if available
              if grep -q "packageRules" "$file"; then
                echo "**Package Rules:**" >> README.md
                grep -A 5 "packageRules" "$file" | grep -E "(matchManagers|matchUpdateTypes|labels|groupName)" | head -6 | sed 's/^/- /' >> README.md
                echo "" >> README.md
              fi
            fi
          done

          cat >> README.md << 'EOF'

          ## 🚀 Quick Start

          Add to your `renovate.json`:
          ```json
          {
            "extends": ["github>trowaflo/renovate-config"]
          }
          ```

          ## 📋 Preset Details

          EOF

          echo "| Preset | Purpose | Auto-merge | Labels |" >> README.md
          echo "|--------|---------|------------|--------|" >> README.md

          for file in *.json5; do
            if [[ -f "$file" ]]; then
              name=$(basename "$file" .json5)
              automerge=$(grep -o '"automerge": true' "$file" | wc -l | tr -d ' ')
              labels=$(grep -o '"labels": \[[^]]*\]' "$file" | head -1 | sed 's/"labels": \[\([^]]*\)\]/\1/' | tr -d '"')
              purpose=$(echo "$name" | sed 's/_/ /g' | sed 's/\b\w/\u&/g')
              echo "| $name | $purpose | $automerge rules | $labels |" >> README.md
            fi
          done

          echo "" >> README.md
          echo "---" >> README.md
          echo "*Documentation auto-generated on $(date)*" >> README.md

      - name: Auto Commit Documentation
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs: 📚 auto-update documentation [skip ci]"
          file_pattern: "README.md"
          commit_author: "GitHub Action <action@github.com>"
